/**
 * Was a verbatim copy of `pages/ui.tsx`; now a re-export of the shared
 * primitives, restyled onto the design system.
 */
export {
  Panel,
  Field,
  inputStyle,
  TabButton,
  PillToggle,
  PrimaryButton,
  SecondaryButton,
  Badge,
} from "../ui";
